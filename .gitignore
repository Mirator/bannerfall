# Copyrighted reference screenshots (Thronefall, Bannerlord, Bad North) — never publish
references/

# Dev-loop artifacts (large, regenerable)
shots/
progress.html
progress.json

# Local tooling
.claude/
__pycache__/

# Browser QA artifacts (large, regenerable)
node_modules/
test-results/
playwright-report/
blob-report/

# scratch Playwright specs (captures, balance sweeps) — see playwright.config.js testIgnore
tests/e2e/zz-*.spec.js

# scratch measurement dumps and one-off probes (regenerable raw output; cited probes and any
# raw JSON whose numbers are quoted in a critiques/ table stay tracked in scripts/ instead)
scripts/zz-*.json
scripts/zz-enemy-command-probe.mjs
scripts/zz-power-fit.mjs
