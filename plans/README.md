# Implementation Plans

Generated by the `improve` skill on 2026-08-17. Plans 001-002 were written from
the audit of `c07be05` and are complete. Plans 003-005 were reconciled against
`d29e284` after that QA foundation landed; they cover selected audit findings
#4, #2, and #5 respectively. Plan 006 was reconciled against `eecb14b` and
consolidates all five vetted performance findings into one phased change.
Execute TODO plans in the order below. Each executor must read its plan fully,
honor its STOP conditions, and update its status row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add a one-command browser QA gate and document it | P1 | M | — | DONE |
| 002 | Cover persistence and non-victory campaign transitions | P1 | M | 001 | DONE |
| 003 | Version, validate, and migrate campaign saves | P1 | M | 002 | DONE |
| 004 | Synchronize live world state before every save | P1 | S | 003 | DONE |
| 005 | Persist a coherent checkpoint before entering battle | P1 | S | 004 | DONE |
| 006 | Eliminate audited scheduler, rendering, simulation, and pathfinding waste | P1 | L | 005 | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line
reason) | `REJECTED` (with one-line rationale).

## Dependency notes

- Plan 002 requires Plan 001 because its browser-level regression tests need the
  Playwright runner, isolated browser contexts, local web-server lifecycle, and
  documented expected-failure policy established there.
- Plan 002 deliberately records three already-confirmed defects as Playwright
  expected failures. It does not fix gameplay code. The later fixes for stale
  autosaves, missing battle-entry checkpoints, and disappearing defeated parties
  must remove the corresponding expected-failure annotations in the same change.
- Plan 003 implements audit finding #4 first because every later writer should
  emit the same versioned shape that `loadRun()` accepts. It adds migration and
  malformed-save coverage without changing gameplay balance.
- Plan 004 implements audit finding #2 on top of Plan 003. It makes the live
  `World` scene the source of truth for hero and roaming-party coordinates at
  persistence time, then retires only the AUDIT-02 expected failure.
- Plan 005 implements audit finding #5 after Plan 004 so battle entry can reuse
  the canonical live-state snapshot path. It retires only AUDIT-05. AUDIT-03
  remains active because ordinary defeat restoration is a separate behavior fix.
- Plan 006 intentionally combines PERFORMANCE-01 through PERFORMANCE-05 because
  scheduler draw gating, render caching/scratch reuse, separation tagging, and
  path-planner amortization share one browser performance characterization gate.
  Its phases must remain separately reviewable even though they ship together.

## Deferred audit findings

- Roaming-party restoration after ordinary defeat (audit finding #3) remains
  separate implementation work and stays represented by an active AUDIT-03
  expected failure.
- Release cache versioning, local-server hardening, and architecture findings
  were not selected for planning yet.

## Findings considered and rejected

- Introduce a gameplay framework or ECS: disproportionate for this small,
  intentionally dependency-free browser game; incremental seams are safer.
- Treat `window.__g` as a security vulnerability: it is a deliberate test/debug
  surface in an offline single-player game. Persistence tests must still use it
  carefully because `window.game` intentionally switches to the test save slot.
- Add a runtime dependency or build step to run QA: rejected. Playwright is a
  development-only dependency; the shipped game remains plain static HTML and ES
  modules.
